import type { CSSProperties } from "react"

export const etaxHeaderStyle: CSSProperties = {
  minHeight: "124px",
  paddingTop: "calc(env(safe-area-inset-top, 0px) + 28px)",
  paddingBottom: "24px",
  boxShadow: "0 2px 8px rgba(0,0,0,0.2)",
}
