# Firebase Admin configuration
# Option 1: paste service account JSON directly
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"anhbao-373f3", ...}

# Option 2: provide local path to service account file (use one of KEY or PATH)
# FIREBASE_SERVICE_ACCOUNT_PATH=./config/anhbao-service-account.json

# Firebase Storage bucket (required for PDF generation)
FIREBASE_STORAGE_BUCKET=anhbao-373f3.appspot.com

# (Optional) Next.js runtime settings
# NEXT_PUBLIC_API_BASE_URL=
